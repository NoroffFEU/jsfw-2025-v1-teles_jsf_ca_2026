import { Spinner } from "@/components/ui/spinner/Spinner";

export const RouteLoader = () => {
  return (
    <div className="flex w-fit mt-40 mx-auto justify-items-center">
      <Spinner />
    </div>
  );
};
